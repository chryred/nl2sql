# AI Configuration
NL2SQL_AI_PROVIDER=openai  # Options: openai, anthropic
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...
# NL2SQL_MODEL=gpt-4o  # Optional: override default model

# Database Configuration
DB_TYPE=postgresql  # Options: postgresql, mysql, oracle
DB_HOST=localhost
DB_PORT=5432  # PostgreSQL: 5432, MySQL: 3306, Oracle: 1521
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=mydb

# Oracle-specific (optional)
# DB_SERVICE_NAME=ORCL  # Oracle service name (if different from DB_NAME)
